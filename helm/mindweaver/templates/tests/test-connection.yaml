apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mindweaver.fullname" . }}-test-connection"
  labels:
    {{- include "mindweaver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mindweaver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
