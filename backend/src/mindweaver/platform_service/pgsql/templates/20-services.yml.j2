apiVersion: v1
kind: Service
metadata:
  name: {{ name }}-pgbouncer-nodeport
  namespace: {{ namespace }}
spec:
  type: NodePort
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: pgbouncer
  selector:
    cnpg.io/poolerName: {{ name }}-pgbouncer
