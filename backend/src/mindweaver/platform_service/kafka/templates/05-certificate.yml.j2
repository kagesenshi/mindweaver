# SPDX-FileCopyrightText: Copyright © 2026 Mohd Izhar Firdaus Bin Ismail
# SPDX-License-Identifier: AGPLv3+

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ name }}-tls
  namespace: {{ namespace }}
spec:
  secretName: {{ name }}-tls
  commonName: {{ name }}.{{ namespace }}.svc.cluster.local
  duration: 2160h # 90d
  renewBefore: 360h # 15d
  subject:
    organizations:
      - mindweaver
  isCA: false
  privateKey:
    algorithm: RSA
    encoding: PKCS1
    size: 2048
  usages:
    - server auth
    - client auth
  dnsNames:
    - {{ name }}
    - {{ name }}.{{ namespace }}
    - {{ name }}.{{ namespace }}.svc
    - {{ name }}.{{ namespace }}.svc.cluster.local
    - {{ name }}-kafka-bootstrap
    - {{ name }}-kafka-bootstrap.{{ namespace }}
    - {{ name }}-kafka-bootstrap.{{ namespace }}.svc
    - {{ name }}-kafka-bootstrap.{{ namespace }}.svc.cluster.local
    - {{ name }}-kafka-external-bootstrap
    - {{ name }}-kafka-external-bootstrap.{{ namespace }}
    - {{ name }}-kafka-external-bootstrap.{{ namespace }}.svc
    - {{ name }}-kafka-external-bootstrap.{{ namespace }}.svc.cluster.local
    - "*.{{ name }}-kafka-brokers"
    - "*.{{ name }}-kafka-brokers.{{ namespace }}"
    - "*.{{ name }}-kafka-brokers.{{ namespace }}.svc"
    - "*.{{ name }}-kafka-brokers.{{ namespace }}.svc.cluster.local"
  issuerRef:
    name: mindweaver-selfsigned-issuer
    kind: ClusterIssuer
    group: cert-manager.io
